name: speed_test_app

services:
  app:
    build:
      context: .
      dockerfile: dockerfiles/app.dockerfile

    ports:
      - "8000:80"

    volumes:
      - ./config:/config

    restart: unless-stopped
